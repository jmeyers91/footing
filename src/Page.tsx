import styled from 'styled-components';
import Column from './Column';

const Page = styled(Column)`
  height: 100vh;
  width: 100vw;
  overflow-x: hidden;
  overflow-y: auto;
`;

export default Page;
